using System;

namespace QuickPlot.Tests.Cookbook
{
    class Program
    {
        /* After the tests run, the output folder is filled with images.
         * This program collects those images, parses the test source code, and creates
         * markdown and HTML documents pairing code with output images.
         * The pairing of short code examples with their output is called a "cookbook".
         */

        static void Main(string[] args)
        {
            Console.WriteLine("cookbook generation complete!");
        }
    }
}
